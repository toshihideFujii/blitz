#![allow(dead_code)]

// A pass which pergorms expansion of the logistic function.
pub struct LogisticExpander {}

impl LogisticExpander {
  pub fn new() {}
  pub fn name() -> String { "logistic-expander".to_string() }
  pub fn run() {}
}