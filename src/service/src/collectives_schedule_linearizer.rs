#![allow(dead_code)]

//use hlo::hlo_module::HloModule;

pub struct CollectivesScheduleLinearlizer {
  //is_enabled: dyn Fn(&HloModule) -> bool
}

impl CollectivesScheduleLinearlizer {
  //pub fn new(is_enabled: dyn Fn(&HloModule) -> bool) -> Self {
    //CollectivesScheduleLinearlizer { is_enabled: is_enabled }
  //}

  pub fn name() -> String { "collectives-schedule-linearlizer".to_string() }
  pub fn run() {}
}