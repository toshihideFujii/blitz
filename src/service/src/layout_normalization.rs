#![allow(dead_code)]

// Normalize shapes for some subsets of HLOs.
pub struct LayoutNormalization {}

impl LayoutNormalization {
  pub fn new() {}
  
  pub fn name() -> String {
    "layout-normalization".to_string()
  }

  pub fn run() {}
}