#![allow(dead_code)]

// The LogBuilder class allows for creating ad-hoc collections
// of records through the `add()` function.
struct LogBuilder {}
impl LogBuilder {
  pub fn new() {}
  pub fn add() {}
  pub fn consume() {}
}