#![allow(dead_code)]

// A cache which stores executables indexed by computation handle
// and version.
struct CompilationCache {}

impl CompilationCache {
  pub fn insesrt() {}

  // Lookup the executable for the specified handle in the cache.
  pub fn lookup() {}
}