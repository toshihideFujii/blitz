#![allow(dead_code)]


struct LeadingZerosCounter {}
impl LeadingZerosCounter {
  pub fn count() {}
}

// Count number of 0's from the most significant bit to the
// least stopping at the first 1.
pub fn countl_zero<T>(_val: T) -> u32 { 0 }