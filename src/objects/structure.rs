// An abstruct superclass, a marker class really, for simple structure classes.
// It doesn't carry an functionality but allows struct classes to be identified
// in the type system.
struct Struct {}

struct Tuple2 {}

struct AccessorPair {}

struct ClassPositions {}
