pub enum Granularity {
  Grapheme, // for character-breaks
  Word,     // for word-breaks
  Sentence, // for sentence-breaks
}

struct JSSegmenter {}

impl JSSegmenter {
  pub fn new() {}

  pub fn resolved_options() {}

  pub fn get_available_locales() {}

  pub fn granularity_as_string() {}

  pub fn set_granularity() {}
  pub fn granularity() {}
  pub fn get_granularity_string() {}
}
